server:
  port: 8081   # usually different from dev

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kodewala
    username: admin
    password: admin786

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true

